# Leave empty in development — Vite proxies /api and /socket.io to localhost:4000.
# Production builds default to https://nexlm-server.vercel.app; set this only
# to point the client at a different API origin.
VITE_API_URL=
